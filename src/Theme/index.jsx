import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  shape: {
    borderRadius: 20, 
  },
  palette: {
    primary: {
      main: '#2A3042',
    },
    secondary: {
      main: '#556EE6',
    },
  },
});

export default theme;
